# 2026-03-07 — Session Notes

## Floating Chat Widget — SHIPPED ✅
- Spec written, Dex reviewed (6/10 first pass), Linus (Opus) + Marcus (Sonnet) built in parallel
- Dex re-reviewed: backend 4/10 → Linus on Opus fixed all 5 issues → 9/10
- **Root cause of widget not rendering**: ChatWidget was in `(dashboard)/layout.tsx` but home page (`/`) is `app/page.tsx` which doesn't use that layout. Fixed by moving to root `layout.tsx` via `ChatWidgetLoader.tsx` wrapper (client component with `dynamic(() => import(...), { ssr: false })` because root layout is server component).
- **`dynamic` naming conflict**: `export const dynamic = 'force-dynamic'` clashed with `import dynamic from 'next/dynamic'`. Renamed to `nextDynamic`, then switched to wrapper pattern.
- **Chat streaming 502 in dev mode**: Next.js dev proxy buffers SSE responses. Fixed by running production mode (`next build && next start`).
- **Chat "404 Page not found" error**: `@ai-sdk/openai` `createOpenAI` sends request format MiniMax rejects. Fixed by **bypassing AI SDK entirely** — raw `fetch` to MiniMax `/chat/completions` + manually emitting `text-start`/`text-delta`/`text-end` JSON event stream format that `DefaultChatTransport` expects.
- **Lesson**: AI SDK v6 `createOpenAI` is NOT compatible with MiniMax M1. Always test LLM integrations with raw fetch first before wrapping in SDK.
- Final commit: `96623d7`

## Team Model Changes
- **Dex promoted to primary builder** (Codex 5.3)
- **Linus swapped to Codex 5.3** (from Sonnet)
- **Marcus stays on Sonnet 4.6** as backup
- **Henry NEVER builds/fixes/reviews code** — specs, architecture, coordination only
- Model selection rules added to AGENTS.md
- **Complex SDK/protocol tasks → Opus**; standard features → Codex/Sonnet

## Workspace Restructured (OpenAI Harness Engineering article)
- **AGENTS.md**: 500→46 lines, table of contents with pointers
- **MEMORY.md**: 500→29 lines, index pointing to split files
- New files: `docs/workspace/WORKFLOW.md`, `GROUP-CHAT-RULES.md`, `HEARTBEAT-GUIDE.md`, `SAFETY.md`
- Split memory: `memory/infrastructure.md`, `architecture.md`, `team.md`, `lessons.md`, `current-work.md`
- Backups at `.bak` files

## Repo Docs Structure — MERGED ✅
- `docs/README.md` — canonical index
- `docs/ARCHITECTURE.md` — system map from actual code
- `docs/DECISIONS.md` — ADR log (18 decisions)
- `docs/QUALITY.md` — scorecard with real test counts (512 pass, 93 fail, 409 skip)
- Linus on Codex scored 9/10

## Nova's Redemption — 5 Tasks Delivered ✅
- File-size CI guard (19 seconds!)
- Skeletons, ErrorBoundary, 404 page, mobile responsive, keyboard shortcuts
- Scored 6.5/10 — two bugs fixed by Linus (favicon location, Cmd+K input guard)
- Nova is viable for small/medium frontend tasks with clear specs

## Track Selection in Review Flow — MERGED ✅
- Dex built `TrackSelectionReviewPanel.tsx` — album grouping, toggles, checkboxes
- Integrates into onboarding flow before "Done"
- Frontend-only, uses existing `PATCH /api/spotify/artists/:id/catalog/selection`
- Commit `644d229`

## GB10 State
- Frontend: production mode (`next start` on port 3000)
- Backend: `tsx watch` on port 3001
- Both healthy (200/307)
- **IMPORTANT**: production build works now (favicon fix landed). Always use `next start` not `next dev` — dev mode breaks SSE streaming proxy.
- Duplicate process issue plagued us all night — multiple deploys left zombie processes. Always `fuser -k 3000/tcp` before starting.

## Cost Tonight (estimate)
- Linus Opus (chat fix): ~$1.20
- Marcus Sonnet (frontend): ~$0.37
- Dex Codex (5 runs): ~$2.50
- Linus Codex (docs + merge + deploy): ~$1.00
- Nova Sonnet (6 tasks): ~$0.50
- Henry Opus (coordination): ~$8-10
- **Total: ~$14-15**

## Pending
- Marcus dual cost view fixes (in progress from before tonight)
- 9 Kobalt reports re-upload
- YC v5 application rewrite
- File-size guard CI (branch exists, needs merge)
- Seb testing all features
- `dex-cleanup-gb10` subagent may still be running (was cleaning processes)

## Afternoon Session (Mar 7, ~16:00-18:20 UTC)

### Artist Profile V1 — BUILT & IN REVIEW
- Dex built `ArtistProfilePanel.tsx` + `GET /api/roster/:id/profile` backend endpoint
- Shows: Spotify header (image, listeners, followers), stats row, tracks table with MLC status badges, stream counts
- Commit `3198bdd` (backend) + `f297f9b` (frontend), merged to main as `7e6156e`
- Artist cards enhanced: track count, stream count (compact K/M/B), registered summary + progress bar

### Unified Artist Profile — IN PROGRESS
- Seb directed: merge old ArtistDashboard (revenue charts) + new ArtistProfilePanel (Spotify/MLC) into ONE page
- Layout: Header → Stats → Revenue Analytics → Spotify Catalog
- Dex built on `feature/unified-artist-profile` (commit `5d9076e`)
- Linus reviewed: 7/10, NEEDS_FIX — track-selection broken, no-op click fallback, unused API call
- Dex fix round 1: `730c0b3` — analytics loading state, null guards, empty state message
- Dex fix round 2: `ffe3800` — track-selection flow restored via "Manage Catalog" button, removed unused getArtistOverview, click fallback added
- **Still needs: final review → merge → deploy**

### Spotify Batch Endpoint Fix — MERGED ✅
- `GET /tracks?ids=...` removed by Spotify Feb 2026
- Dex switched to individual `GET /tracks/{id}` with 200ms throttle
- Commit `fcb3ec2`, merged to main

### MLC Scanner Panel — MERGED ✅
- Full detail panel with sidebar nav (🔍)
- Table of ISRCs with color-coded status badges
- Commit `f297f9b`, merged as `34b3af9`

### Jaques Research: Spotify API + Registries
- Output: `OBSIDIAN/Jaques/SPOTIFY-API-AND-REGISTRY-RESEARCH.md`
- Key findings: batch endpoints removed Feb 2026, no real stream counts from Spotify (sp-playcount is unofficial), audio-features dead
- SoundExchange: free ISRC search at isrc.soundexchange.com, no API, scrapeable
- Songview (BMI/ASCAP joint): no API, web-searchable
- We already store most available data via sp-playcount

### Process Lessons (Seb corrections)
- **Don't read diffs on Opus** — delegate code review to Sonnet/Codex agents. Henry got called out for reviewing 500+ lines of diffs directly.
- **Auto-review pipeline**: when builder delivers, IMMEDIATELY spawn reviewer. Don't wait for Seb to say "review." Added to `docs/workspace/WORKFLOW.md`.
- **28 min dead time between Dex delivery and review start** — Seb noticed. Unacceptable. Pipeline must be: build → auto-review → validate → merge.
- **Cost discipline still slipping** — Henry still doing git operations, reading files, SSH commands on Opus.

### Entity Resolution + MLC Scan
- Manually resolved 114 CanonicalTracks (script `backend/scripts/resolve-all.ts`)
- MLC scan: 114 scanned, 57 NO_ISWC, 57 NOT_IN_MLC, $427,954 estimated unclaimed
- Auto entity resolution after upload now deployed (Dex, commit `9458330`)

### DB State
- 3 roster artists (Gonzalo Ávila, NADIRA, Vyse), all linked to CanonicalArtist
- 672 SpotifyCatalogTracks, 114 CanonicalTracks, 114 MLCScanResults
- 64,227 royalty entries, 1 report, $24,663.40 revenue
- 12 unlinked names (VIUS $18K, BLANKO $1.3K, etc.)

### Spec Written
- `docs/TASK-ARTIST-PROFILE-V1.md` — committed to main

### Pending
- Merge unified artist profile after fix review
- Deploy to GB10
- Dex E2E test results (spawned but may have timed out)
- Marcus dual cost view fixes still pending

# 2026-03-13 — Session Notes

## Fixes Shipped (all merged to main, deployed to GB10)

### 1. Roster Artist Backfill (`e360b51`)
- **Branch:** `fix/roster-artist-backfill` (Linus, GPT-5.4)
- **New file:** `backend/src/services/rosterLinkingService.ts`
- `relinkEntriesForRosterArtist(rosterArtistId, orgId)` — name-based + ISRC-based linking against all unlinked RoyaltyEntry rows
- `relinkAllEntriesForOrg(orgId)` — admin bulk relink
- Hooked into `createRosterArtist()` and `updateRosterArtist()` (fire-and-forget with .catch)
- **Fixes:** Adding artist after report import now auto-links existing royalty entries

### 2. Chat Cleanup on Logout (`21f2c94`)
- **Branch:** `fix/chat-cleanup-on-logout` (Dex, GPT-5.4)
- Added `clearMessages` action to Zustand store
- `useAuth()` watches `userId`/`orgId` — clears on change (uses authSessionRef to skip first render)
- Trash icon button in widget header for manual clear
- **Hotfix on GB10:** `clearMessages` only cleared Zustand store, not AI SDK's `useChat` messages. Fixed by adding `handleClearChat` that calls both `clearMessages()` and `setChatMessages([])`. This fix is only on GB10, not committed to git yet.

### 3. Entity Cleanup on Report Delete (`f6d18d6`)
- **Branch:** `fix/cleanup-orphaned-entities-on-report-delete` (Dex, GPT-5.4)
- Added `cleanupOrphanedEntities(orgId)` to `forensicCleanupService.ts`
- Deletes orphaned CanonicalTrack (by ISRC/title match) and CanonicalArtist (no RosterArtist, no SpotifyCatalogTrack, no matching RoyaltyEntry)
- Called after `invalidateForensicDataForOrg()` in `catalogStore.delete()`
- **Fixes:** Issues page now clears entity conflicts when reports are deleted

## Issues Found
- **OpenAI API quota exceeded** — GPT-4o-mini key out of credits. Chat returns "Something went wrong". Need Seb to top up or switch to MiniMax.
- **Chat display tools inconsistent** — LLM non-deterministically decides whether to call `display_track_cards` (album art) or plain text. Fix: update system prompt in `chatAgent.ts` to explicitly instruct when to use display tools.

## Pending (not committed to git)
- `handleClearChat` hotfix on GB10 ChatWidget.tsx — needs to be committed properly
- System prompt update for consistent display tool usage
- Entity resolution investigation from Jaques: documented full pipeline gaps (autoLinkEntries only runs at upload time)

## Git State
- main HEAD: `f6d18d6`
- GB10 has additional uncommitted hotfix in ChatWidget.tsx

## Jaques Research: Entity Resolution Pipeline
- Entity resolution and revenue attribution are separate systems
- `autoLinkEntries()` runs only at report upload time
- Adding/updating roster artist now triggers `relinkEntriesForRosterArtist` (fixed above)
- Missing triggers still: deleting a report doesn't unlink entries from roster artists (entries are deleted so moot), re-importing doesn't re-resolve already-linked entries
